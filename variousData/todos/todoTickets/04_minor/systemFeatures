add lookup> mark szstems as favorite, hide, pagination for lookup

"favorite"
or enabled in annotator (when more systems show up)


PROBABLY:

  PRIMARY (can be changed anytime; always shows up in searches, default in annotator)
  FAVORITE 
  (UNSELECTED) (not listed in annotator, perhaps not even in searches)
  HIDE